import { EtablissementPipe } from './etablissement.pipe';

describe('EtablissementPipe', () => {
  it('create an instance', () => {
    const pipe = new EtablissementPipe();
    expect(pipe).toBeTruthy();
  });
});
